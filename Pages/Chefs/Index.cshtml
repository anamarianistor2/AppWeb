@page
@model AppWeb.Pages.Chefs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Chef[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Chef[0].LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Chef) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<div  style="max-width: 60%; margin: 0 auto;">
    <img src="https://ik.imagekit.io/munchery/blog/tr:w-768/7-outstanding-chefs-shaping-mexican-cooking-olvera-camara-quintana-plascencia-soto-innes-gaytan-and-guzman.jpeg" alt="food" style="max-width: 100%;" />
</div>
