@page
@model AppWeb.Pages.Reservations.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Reservation.ID" />
            <div class="form-group">
                <label asp-for="Reservation.ClientID" class="control-label"></label>
                <select asp-for="Reservation.ClientID" class="form-control" asp-items="ViewBag.ClientID"></select>
                <span asp-validation-for="Reservation.ClientID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.LocationID" class="control-label"></label>
                <select asp-for="Reservation.LocationID" class="form-control" asp-items="ViewBag.LocationID"></select>
                <span asp-validation-for="Reservation.LocationID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.ReservationDateTime" class="control-label"></label>
                <input asp-for="Reservation.ReservationDateTime" class="form-control" />
                <span asp-validation-for="Reservation.ReservationDateTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
